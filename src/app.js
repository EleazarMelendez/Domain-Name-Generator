/* eslint-disable */
import "bootstrap";
import "./style.css";

import "./assets/img/rigo-baby.jpg";
import "./assets/img/4geeks.ico";

window.onload = function generateDomain(a, b, c) {
  for (let i = 0; i < a.length; i++) {
    let element1 = a[i];
    return element1;
  }
  for (let j = 0; j < b.length; j++) {
    let element2 = b[i][j];
    return element2;
  }
  cnt3;
};

document.write(generateDomain(pronoun, adj, noun));
